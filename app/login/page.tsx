import LoginForm from "@/app/_components/LoginForm";

export const metadata = { title: "Sign in — RotoWire NHL Social Hub" };

export default function LoginPage() {
  return (
    <main className="wrap">
      <LoginForm />
    </main>
  );
}
